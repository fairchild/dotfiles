# Legacy runtime locations retained only so old installs cannot publish them.
# Current generated runtime lives in ~/.agents/ outside the checkout.
/skills/
/runtime-backups/

# Machine-local plugin configuration and scratch handoffs never belong in public source.
/plugins/
/todo.md

# Skill-local runtime histories and generated artifacts.
**/data/*.jsonl
**/outputs/*
!**/outputs/README.md
